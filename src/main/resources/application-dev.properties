## DATABASE CONNECTION ##
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:skydivingrest1}?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:userdb66}
spring.datasource.password=${MYSQL_PASSWORD:D$chool12#4566}

#spring.jpa.properties.hibernate.show_sql=true

## JPA # auto-update schema for local dev
spring.jpa.hibernate.ddl-auto=update

## Logging # reduce noisy logs
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

